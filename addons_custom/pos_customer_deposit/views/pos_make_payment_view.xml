<?xml version="1.0"?>
<odoo>
    <data>
        <record id="izi_view_pos_payment_deposit" model="ir.ui.view">
            <field name="name">izi.pos.make.payment.form</field>
            <field name="model">pos.make.payment</field>
            <field name="arch" type="xml">
                <form string="Pay Order">
                    <group>
                        <field name="journal_id" context="{}" options="{'no_create': True, 'no_open': True, 'no_edit': True}"/>
                        <field name="amount" widget="monetary"/>
                        <field name="payment_name"/>
                        <field name="session_id" invisible="1"/>
                    </group>
                    <footer>
                        <button name="action_deposit" context="{'pos_customer_deposit_line_id': active_id}" string="Tạo thanh toán" type="object"
                                class="btn-primary"/>
                        <button special="cancel" string="Huỷ" class="btn-default"/>
                    </footer>
                </form>
            </field>
        </record>
        <record id="action_pos_payment_deposit" model="ir.actions.act_window">
            <field name="name">Payment</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">pos.make.payment</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>